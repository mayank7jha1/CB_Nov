#include<bits/stdc++.h>
using namespace std;
// const int N = 0;

void Solve(int a[], int n) {

}

int main() {
	int n;
	cin >> n;
	int a[n];
	for (int i = 0; i < n; i++) {
		cin >> a[i];
	}

	cout << a << endl;
	//Address of the first block or we say
	//base addres of the array.

	Solve(a, n);
}