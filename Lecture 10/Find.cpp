#include<bits/stdc++.h>
using namespace std;
const int N = 0;;

int main() {
	string s, t;
	cin >> s >> t;

	cout << s.find(t);//Main s ke ander t ko find karna chahta hu.
	//This find function returns the index of the first letter ka jaha se element mila hain.

	//STL:s.find()



	//Find Algorithm: This return the first index where the 2 second is found in
	//string 1.
	//And if the output is some random number or something that is
	//larger that the size of the string 1 then you can say that string 2 is not
	//found in string 1.


	//IN find function of stl you are trying to find the first occurence
	//of string 2 in string 1.

	//s.find(t);; Here s is your string 1 and t is your string 2.
	//you are trying to find the first occurence of string t in string s.
}